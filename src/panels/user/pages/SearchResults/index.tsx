import {SearchBox} from "@/panels/Components";

const SearchResultsPage = () => {
    return (
        <div>
            <SearchBox
                autoFocus={true}
            />
        </div>
    );
};

export default SearchResultsPage
